هب انم خدا
مستند توضیحات امنیت:
 : AES کالس-۱
در ابتدا در کالس AES دو فیلد داریم : secretKey & Key که توضیح داده می شوند:
Key :که ارایه ای از بایت هاست و در تابع setKey عملیات روی آن انجام میشود و در نهایت با پیاده سازی
الگوریتم AES رو آن، secretKey ست و تنظیم می شود.
نام کالس برگرفته از روش ( Standard Encryption Advanced(AES است که عملیات رمز نگاری را در
آن انجام می دهیم. حاال به بررسی توابع موجود در این کالس می پردازیم:
１ -تابع (myKey String(SetKey : تابعی است که با گرفتن یک رشته از ما به عنوان کلید
ابتدایی و پردازش و انجام یک سری عملیات که توضیح داده خواهند شد، در نهایت یک کلید نهایی
سری را ست می کند. در ابتدا در این تابع باید از کالس MessageDigest نمونه گیری شود و در
کانستراکتور آن نوع الگوریتم برای عمل رمز نگاری را مشخص شود. سپس با توابع و متد های متنوع
موجود در کالس MessageDigest یک کلید جدید بسازیم.)توابعی نظیر
 )digest(key) یا Array.copyof(key,newLength)-
２ -تابع (token String(getSecretKeyByToken : این تابع با گرفتن ت وکن ار هر کاربر که
یک عدد یکتاست و انجام عملیات روی رشته توکن ، یک کلید ثانویه می سازد.)عملیات را طوری ترتیب
می دهیم که به سادگی قابل ردیابی نباشد.(
３ -تابع (secretKey,strToEncrypt String(encrypt : آرگومان اول این تابع، رشته ای
است که قرار است کد گذاری شود. رشته دوم، همان کلید ثانویه ای است که در تابع
getSecretKeyByToke ساخته شد. سپس تابع SetKey را با ورودی secretKey صدا میزنیم.
در این تابع، از کالس Cipher استفاده شده است که بعدا به آن خواهیم پرداخت.
４ -تابع (secretKey,strToDecrypt String(decrypt : همان کار های encrypt را انجام
میدهد با این تفاوت که کد گشایی می کند و عملیات برعکس است.
 :Cipher کالس-۲
این کالس برای عملی ساختن decryption & encryption ساخته شده است. در ابتدا باید یک نمونه از آن
گرفته شود و در سازنده آن نوع الگوریتم را مشخص کنیم. سپس باید تابع init را صدا بزنیم که ساز و کار آن
مطابق شکل زیر است:
Init(Cipher_Mode,String secretKey)
که secretKey درآن برابر همان کلیدی است که در تابع setKey آنرا مشخص و ست کرده ایم. سپس با تابع
doFinal رشته نهایی را بر میگردانیم. 